@model IEnumerable<BudgetToSave.Models.DonationLocation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/StandardLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Donation.DonationID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Donation.DonationID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonationLocation1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonationLocation1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DonationLocation1 })
        </td>
    </tr>
}

</table>
